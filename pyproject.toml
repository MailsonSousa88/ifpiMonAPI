[project]
name = "ifpimonapi"
version = "0.1.0"
description = "API de consulta de IFPI Mons desenvolvida com FastAPI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
]
